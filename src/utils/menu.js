/**
 * 定义本地菜单，远程获取的话请到config修改为服务端渲染，且字段名与这里的字段一样
 * 如需修改可以修改字段名，修改之后请到Menu、Bread组件对各字段进行修改
 * module 对应的模块名，主键，首字母最好大写
 * @title 标题
 * @class 图标名，antd的图标
 * @sub 子项 Array
 * 例：
 * {
    module: 'UserManage',
    title: '用户管理',
    class: 'user',
    sub: [
      {
        module: 'UserList',
        title: '用户管理',
      },
    ],
  },
 */

module.exports = [
  
];
